{
  "name": "@keytiles/keytiles-private-apis-generator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf publish/dist",
    "tscBuild": "tsc --build",
    "build": "npm run clean && npm run tscBuild",
    "prepare": "npm run build"
  },
  "homepage": "https://keytiles.com",
  "devDependencies": {
    "axios": "^1.11.0",
    "orval": "7.10.0 - 7.10.9999",
    "rimraf": "6.0.1 - 6.0.9999",
    "typescript": "^5.8.3",
    "@keytiles/orval-importmapping-support": "^1.0.1"
  },
  "dependencies": {
  }
}
