{
  "name": "@keytiles/keytiles-public-api-generator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf publish/dist",
    "tscBuild": "tsc --build",
    "build": "npm run clean && npm run tscBuild",
    "prepare": "npm run build"
  },
  "homepage": "https://keytiles.com",
  "devDependencies": {
    "axios": "^1.11.0",
    "typescript": "^5.8.3",
    "orval": "^7.10.0",
    "rimraf": "^6.0.1"
  }
}
